-- PATH=$PATH:/Applications/MAMP/Library/bin
-- export PATH
-- mysql -h localhost -u root -p food < Veggies_and_fruits.txt

CREATE TABLE Veggies_and_fruits (
ID int NOT NULL AUTO_INCREMENT,
name VARCHAR(30),
calories VARCHAR(30),
img VARCHAR(150),
PRIMARY KEY (ID)
);